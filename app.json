{
  "name": "Telegram News Bot",
  "description": "A Telegram bot that fetches the latest Sri Lanka Helakuru news using NewsData.io API",
  "repository": "https://github.com/TEAM-DLK/NewsAPi",
  "keywords": ["telegram", "bot", "news", "python"],
  "buildpacks": [
    { "url": "heroku/python" }
  ],
  "env": {
    "TELEGRAM_BOT_TOKEN": {
      "description": "Your Telegram Bot Token from BotFather",
      "required": true
    },
    "NEWS_API_KEY": {
      "description": "Your API key from NewsData.io",
      "required": true
    }
  }
}